Make it work in local mode again
  (by default app.config has a APPLICATION_ROOT
  config key and it' s None)

Write some tests

factorize link generation in templates
(something like the link_to(url, category) I had
in rfortunes)
